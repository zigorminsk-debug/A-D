<?xml version="1.0" encoding="utf-8"?>
<!-- Баннер напоминания о событии: отображается поверх экрана/рабочего стола.
     Кнопка «Открыть» открывает нужное действие в приложении.
     Кнопка «Отложить на 30 минут» закрывает баннер и планирует повтор со звуком. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#66000000"
    android:gravity="center"
    android:padding="20dp">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardReminder"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:clickable="true"
        android:focusable="true"
        app:cardBackgroundColor="@color/white"
        app:cardCornerRadius="24dp"
        app:cardElevation="10dp"
        app:strokeColor="@color/stroke"
        app:strokeWidth="1dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <!-- Заголовочная строка: иконка + бейдж типа + время -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <ImageView
                    android:id="@+id/ivIcon"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:background="@drawable/bg_ok"
                    android:padding="8dp"
                    android:src="@drawable/ic_heart"
                    app:tint="@color/primary"
                    tools:ignore="ContentDescription" />

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="12dp"
                    android:layout_weight="1"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/tvBadge"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:fontFamily="sans-serif-medium"
                        android:text="@string/reminder_banner_badge_measure"
                        android:textColor="@color/primary"
                        android:textSize="13sp" />

                    <TextView
                        android:id="@+id/tvTime"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textColor="@color/gray"
                        android:textSize="12sp"
                        tools:text="08:00" />
                </LinearLayout>
            </LinearLayout>

            <!-- Основной заголовок (что делать/какой препарат) -->
            <TextView
                android:id="@+id/tvTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="14dp"
                android:fontFamily="sans-serif-medium"
                android:textColor="@color/textDark"
                android:textSize="20sp"
                tools:text="Пора измерить давление 🩺" />

            <!-- Пояснение к действию -->
            <TextView
                android:id="@+id/tvActionHint"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:lineSpacingExtra="2dp"
                android:textColor="@color/textDark"
                android:textSize="14sp"
                tools:text="Измерить артериальное давление и записать в дневник" />

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginTop="18dp"
                android:layout_marginBottom="14dp"
                android:background="@color/stroke" />

            <!-- Кнопки: Открыть (первичная) и Отложить на 30 минут -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnOpen"
                style="@style/Widget.BPDiary.Button"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="52dp"
                android:text="@string/reminder_banner_open"
                android:textSize="16sp"
                app:icon="@drawable/ic_open_in_new"
                app:iconGravity="textStart"
                app:iconTint="@color/onPrimary" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnSnooze"
                style="@style/Widget.BPDiary.Button.Outlined"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:minHeight="48dp"
                android:text="@string/reminder_banner_snooze"
                android:textSize="14sp"
                app:icon="@drawable/ic_snooze"
                app:iconGravity="textStart"
                app:iconTint="@color/primary" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>
</FrameLayout>
